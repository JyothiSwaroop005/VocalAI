/**
 * AudioPlayerWidget.tsx — DEPRECATED
 *
 * This component has been replaced by MusicPlayerWidget.tsx which uses
 * honest YouTube embeds instead of Web Audio API synthesis.
 *
 * Kept as an empty stub to avoid any lingering import errors.
 * Safe to delete after confirming no references remain.
 */

export const AudioPlayerWidget = () => null;
